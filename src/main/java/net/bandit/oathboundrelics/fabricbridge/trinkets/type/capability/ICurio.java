package net.bandit.oathboundrelics.fabricbridge.trinkets.type.capability;

public interface ICurio {
    enum DropRule {
        DEFAULT,
        ALWAYS_KEEP,
        ALWAYS_DROP,
        DESTROY
    }
}